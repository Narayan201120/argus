{"_note": "Empty v0.6.0 baseline: no live run has populated per-case aggregates yet. The first `scripts/eval_live.py --live` run fills `cases`; until then diffs compare over an empty intersection and trivially pass.", "_schema": {"cases": "map of case_id -> per-case aggregate", "cases.<id>.claim_count": "int >= 0", "cases.<id>.elapsed_s": "float|null seconds from POST to terminal; null when unknown", "cases.<id>.failures": "list[str]; empty means the case passed validation", "cases.<id>.grounded_ratio": "float in [0.0, 1.0]; 1.0 when the board has no claims", "cases.<id>.has_final_synthesis": "bool; any synthesis with final == true", "cases.<id>.refused_correctly": "bool DEC-054 honest-answer-shape heuristic (zero fabrication)", "cases.<id>.terminal": "complete|failed|budget_exhausted|cancelled|null", "version": "baseline label, e.g. v0.6.0"}, "cases": {}, "version": "v0.6.0"}
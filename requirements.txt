# Web framework
# Upper caps = majors verified by the test suite; bump deliberately.
fastapi>=0.115.0,<1
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9  # multipart/form-data (audio uploads)

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Async HTTP
httpx>=0.27.0

# Caching / rate limiting
redis>=5.0.4

# AI provider SDKs
google-generativeai>=0.7.0,<0.9
openai>=1.35.0,<4
anthropic>=0.30.0,<2
mistralai>=1.0.0,<3

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
httpx>=0.27.0  # also used for TestClient
fakeredis>=2.23.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Auth
pyjwt>=2.8.0

# Metrics
prometheus-client>=0.20.0,<1

# Tracing (opt-in; OTLP exporter installed separately if needed)
opentelemetry-api>=1.20,<2
opentelemetry-sdk>=1.20,<2

# Tooling (CI + local stage gate)
ruff>=0.5.0
mypy>=1.10.0
